pipeline {
    agent any
    stages {
        stage('Debug') {
            steps {
                sh '''
                    echo "Current directory: $(pwd)"
                    ls -al
                    git --version
                    git config --get remote.origin.url || echo "No git repo"
                '''
            }
        }
    }
}